generator client {
  provider = "prisma-client"
  output   = "../api/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model User { 
  id          String  @id @default(uuid()) 
  email       String  @unique
  username    String?
} 
